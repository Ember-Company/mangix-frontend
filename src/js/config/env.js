// temporary solution, switch to environment variables later
const app_config = Object.freeze({
  ENVIRONMENT: "dev",
  BASE_PREFIX: "http://localhost:5173",
  APP_PAGES_PREFIX: "http://localhost:5173/pages",
});

export default app_config;
